# Extracted from: //www.rabbitmq.com/install-debian.html

Instalacion en Debian/Ubuntu:

	apt-get install rabbitmq-server (queue manager)
	pip3 install pika (producers and consumers)


System overview:
	Producer (P)
	Consumer (C)
	Message queue: hello 
	
	P --> ||||||m3|m2|m1| --> C
		hello

Consumer:
	
	python3 receiver.py

Producer:
	
	python3 sender.py
